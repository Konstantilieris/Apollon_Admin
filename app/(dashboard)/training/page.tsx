import ConstantSwitcher from "@/components/shared/constantManagement/ConstantSwitcher";
import { getConstant } from "@/lib/actions/constant.action";

import React from "react";

const Page = async () => {
  return (
    <section className="flex h-full w-full flex-col items-center justify-center p-8 font-sans">
      training
    </section>
  );
};

export default Page;
